spring:
  application:
    name: config.server 
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dancourbano/springcloudmicroservice
#HTTP Server
server:
  port: 4444

  